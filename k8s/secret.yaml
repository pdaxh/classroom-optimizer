apiVersion: v1
kind: Secret
metadata:
  name: classroom-optimizer-secrets
  labels:
    app: classroom-optimizer
type: Opaque
stringData:
  # Replace with actual Google ADK API key
  # kubectl create secret generic classroom-optimizer-secrets --from-literal=google-api-key=your-key-here
  # NOTE: For production, use Sealed Secrets (see ../clara/k8s/README-SECRETS.md for reference)
  google-api-key: ""

